<% imgHelper=(folder,imageName,className ='img-avatar',alt = 'Avatar')=> {
    if (imageName == null) {
        imageName = 'noAvatar.png';
    }
    return `<img class="${className}" src="${folder+imageName}" alt="${alt}"/>`
}
%>